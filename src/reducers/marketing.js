/*import {
    SWITCH_CURRENT_COMPONENT
} from '../constants/ActionTypes'*/

import { CAMPAIGN_MEDIA_TYPES } from '../constants/MarketingConstants'

const initialState = CAMPAIGN_MEDIA_TYPES

export default function campaignMediaTypes(state = initialState, action) {
    return state
}
